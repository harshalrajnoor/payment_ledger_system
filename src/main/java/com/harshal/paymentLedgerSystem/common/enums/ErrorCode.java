package com.harshal.paymentLedgerSystem.common.enums;

public enum ErrorCode {
    VALIDATION_ERROR,
    RESOURCE_NOT_FOUND,
    INSUFFICIENT_BALANCE,
    DUPLICATE_REQUEST,
    WALLET_INACTIVE,
    CURRENCY_MISMATCH,
    INTERNAL_ERROR
}
