spring:
  application:
    name: paymentLedgerSystem

  datasource:
    url: jdbc:postgresql://localhost:5433/payment_ledger
    username: ${DB_USERNAME:harshal}
    password: ${DB_PASSWORD:678876}
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update

    show-sql: true

    properties:
      hibernate:
        format_sql: true

  sql:
    init:
      mode: always

server:
  port: 8080